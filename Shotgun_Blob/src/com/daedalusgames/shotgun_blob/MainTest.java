package com.daedalusgames.shotgun_blob;

import org.jbox2d.common.Vec2;
import junit.framework.TestCase;

/**
 * // -------------------------------------------------------------------------
/**
 *  Provides test cases for the Main class.
 *
 *  @author Artur
 *  @version Sep 25, 2012
 */
public class MainTest extends TestCase
{
    /**
     * Empty constructor for tests.
     * Initialization is done in the setUp method instead.
     */
    public MainTest()
    {
        //Empty constructor for tests.
    }

    public void setUp()
    {
        //Empty setup.
        //Variables and methods are static.
    }

    /**
     * Tests the getWorld method.
     */
    public void testGetWorld()
    {
        //assertNotNull(Main.getWorld());
    }

    /**
     * Tests the gravity getter and setter methods.
     */
    public void testGravity()
    {
        //assertEquals(new Vec2(0.0f, -10.0f), Main.getGravity());
        //Main.setGravity(new Vec2(0.0f, 3.33f));
        //assertEquals(new Vec2(0.0f, 3.33f), Main.getGravity());
    }
}
